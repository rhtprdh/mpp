message: "Hello from Config Server"

server:
  port: 8881

spring:
  application:
    name: config-server
  
  cloud:
    config:
      server:
        git:
          uri: file:///C:/config-repo
          default-label: main

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
